start:

  string x;
  string y;
  string z="informatika";
  x="ppj2024";
  std_output << x;
  std_error <<  z;
  std_error <<  z+z;
  std_error <<  x;
  std_error <<  x+x;
  std_output << x* "ppj20";  
  
  std_error << "bg matf";
  x="ub";

  x="abcdef";
  y="def";
  std_error << "bg matf 2";

  std_error << "bg matf 3";
  print_ALL;
  if(x==y){
    std_output << "Then!";
  } else {
    std_error << "Else!";
  }
  ;

  string w;
  std_input >> w;
  std_output << w;
  std_error << w;
  std_output << w+w;
  std_output << w*"oop";
  std_output << "oop" * "os";
  x="a";
  y="aaaaa";
  string add="a";
  while(x!=y){
    std_output << "We are still in the loop!";
    x=x+add;
  }
  ;

end
